// ============================================
// INTERFACE ACTOR
// ============================================
// Define la estructura de datos de un actor.
// Ejemplo: Tom Hanks, Meryl Streep, etc.

export interface Actor {
  id: string;
  name: string;
  created_at?: string;
}
